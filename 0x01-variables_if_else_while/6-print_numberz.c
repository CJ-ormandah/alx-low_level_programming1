#include <stdio.h>
#include <stdlib.h>

/**
 * main - returns all alphabets except e and q
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int a = 0;

	for (; a < 10 ; a++)
		putchar('0' + a);
	putchar('\n');
	return (0);
}
